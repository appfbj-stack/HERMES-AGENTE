# PROBLEMA IDENTIFICADO!

O arquivo MasterPanel.tsx foi quebrado durante as minhas modificações e precisa ser reconstruído.

## Causa:
O Coolify está pegando o arquivo do repositório git, que não tem as minhas modificações locais.

## SOLUÇÃO:

Você tem 2 opções:

### Opção 1: Commit e Push das mudanças (RECOMENDADO)

```bash
git add .
git commit -m "feat: add Hermes Admin Master"
git push
```

Depois faça redeploy no Coolify.

### Opção 2: Manually no Coolify

Vá ao repositório no GitHub e edite o arquivo MasterPanel.tsx diretamente, adicionando:

1. O botão Hermes Admin (após o botão "+ Novo cliente")
2. O componente HermesAdminPanel (dentro do return, antes do fechamento)

---

## Código que precisa ser adicionado:

### 1. Adicionar botão (após linha 344):
```tsx
<button
  onClick={() => setShowHermesAdmin(true)}
  className="rounded-full bg-violet-600 px-5 py-2 text-sm font-semibold text-white hover:bg-violet-700"
>
  Hermes Admin
</button>
```

### 2. Adicionar component (antes do fechamento do return):
```tsx
<HermesAdminPanel
  show={showHermesAdmin}
  tab={hermesTab}
  setTab={setHermesTab}
  onClose={() => setShowHermesAdmin(false)}
/>
```

---

## Arquivos que precisam ser commitados:

- frontend/src/MasterPanel.tsx
- frontend/src/api.ts
- frontend/src/types.ts
- backend/app/models.py
- backend/app/schemas.py
- backend/app/services/hermes_admin.py
- backend/app/routes/admin_hermes.py
- backend/migrations/003_hermes_admin.sql
- backend/migrations/004_admin_skills.sql

---

Me avise qual opção você prefere!
